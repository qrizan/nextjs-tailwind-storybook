import { ICategory } from './Category';

const base: ICategory = {
  name: 'Category',
  slug: 'category',
  image: 'https://fakeimg.pl/200x200/?text=Category',
};

export const mockCategoryProps = {
  base,
};